# 安全政策

## 支持范围

当前维护 `main` 分支。GitHub Pages 在线评审版仅用于固定合成数据的浏览器内流程演示，不是生产系统，也不连接项目的 FastAPI、SQLite 或 DeepSeek。

## 报告漏洞

请使用 GitHub 仓库 Security 页的私密漏洞报告功能，不要在公开 Issue 中披露漏洞、凭据或可利用细节。报告请包含：

- 受影响的提交或版本；
- 最小复现步骤；
- 影响范围；
- 建议缓解方式（如有）。

请勿针对公开预览执行压力测试、凭据探测或尝试绕过静态评审边界。Pages 状态只存在当前浏览器，不存在可供访问的其他访客业务数据。

## 明确边界

- 不会要求贡献者提供 DeepSeek 或 OpenAI API Key；离线回放是默认开发路径。
- Pages 构建不得配置模型 Key、后端数据库、附件、企业数据或个人数据。
- 发现疑似密钥泄露时应立即撤销并轮换，而不是只删除 Git 文件。
